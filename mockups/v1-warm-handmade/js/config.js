/* ============================================================
   warmprint — Project Config
   Fill in real contacts before deploy
   ============================================================ */

window.WARMPRINT_CONFIG = {
  contacts: {
    telegram:  '@warmprint',
    instagram: '@warmprint',
    email:     'hello@warmprint.store'
  },
  defaultLang: 'ru',
  localStorageKey: 'warmprint-lang'
};
